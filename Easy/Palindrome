class Solution {
    public boolean isPalindrome(int x) {
        int rev=0;
        int temp=x;
        while(temp>0){
            int n=temp%10;
            rev=rev*10+n;
            temp=temp/10;
        }
        return(rev==x);
        
    }
}